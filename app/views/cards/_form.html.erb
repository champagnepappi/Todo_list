<%= form_for([@list, @list.cards.build]) do |f| %>
    <%= f.text_field :description, placeholder: "Card description .." %>
    <%= f.hidden_field :user_id, value: current_user.id %>
    <%= f.hidden_field :list_id, value: @list.id %>
    <%= f.submit "submit", class:"button" %>
<% end %>

