<p>
  <strong>Title:</strong>
  <%= @list.title %>
</p>

<p>
  <strong>Description:</strong>
  <%= @list.description %>
</p>
    <%= link_to @card.description %>
<%= render 'cards/form' %>

<%= link_to 'Edit', edit_list_path(@list) %> |
<%= link_to 'Back', current_user %>
